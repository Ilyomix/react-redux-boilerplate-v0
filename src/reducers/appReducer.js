// @flow
import type { Action } from '../types/redux-types';

const initialState = {
};

type State = {}

// eslint-disable-next-line no-unused-vars
const rootReducer: (state: State, action: Action) => State = (state: State = initialState, action: Action) => state;

export default rootReducer;
